import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const primary = Color(0xFFFF6B00);
  static const primaryLight = Color(0xFFFF8A3D);
  static const secondary = Color(0xFFFFB74D);

  // Background
  static const background = Color(0xFF0F1115);
  static const surface = Color(0xFF1A1D24);
  static const card = Color(0xFF242833);

  // Text
  static const textPrimary = Colors.white;
  static const textSecondary = Color(0xFFB8BDC9);

  // Status
  static const success = Color(0xFF22C55E);
  static const warning = Color(0xFFF59E0B);
  static const error = Color(0xFFEF4444);

  // Misc
  static const border = Color(0xFF343A46);
}